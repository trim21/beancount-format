// NOTE: The old inline formatting tests were migrated to fixture-based integration tests.
// See `tests/format_cases.rs` and fixtures in `tests/format-and-check/`.

#[test]
fn migrated_to_fixtures() {
  // This test exists only to make the migration explicit.
  assert!(true);
}
