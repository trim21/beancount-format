pull_request_rules:
  - name: label conflicts
    conditions:
      - conflict
    actions:
      label:
        toggle:
          - conflict

  - name: Automatic merge on approval
    conditions:
      - '#commits-behind=0'
      - or:
          - '#approved-reviews-by>=1'
          - label=ci:auto-merge
      - or:
          - not: 'head*=renovate/lockfile-maintenance-*'
          - 'created-at<=24h ago'
      - base=master
      - check-success=cargo-lint-test
      - check-success=dprint-test
      - check-success=python-test
      - check-success=pre-commit
    actions:
      merge:
        method: squash
